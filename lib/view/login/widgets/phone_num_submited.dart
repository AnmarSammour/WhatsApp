import 'package:flutter/material.dart';

class PhoneSubmitWidget extends StatelessWidget {
  const PhoneSubmitWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}