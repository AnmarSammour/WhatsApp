import 'package:flutter/material.dart';

class ChangeGroupName extends StatelessWidget {
  const ChangeGroupName({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}