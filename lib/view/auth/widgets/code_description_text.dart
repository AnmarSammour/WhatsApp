import 'package:flutter/material.dart';

class CodeDescriptionText extends StatelessWidget {
  const CodeDescriptionText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Enter 6-digit code',
      textAlign: TextAlign.center,
    );
  }
}
