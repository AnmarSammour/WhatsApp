import 'package:flutter/material.dart';

class AddAttachFile extends StatelessWidget {
  const AddAttachFile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}