import 'package:flutter/material.dart';

class ChatBodyGroup extends StatelessWidget {
  const ChatBodyGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}