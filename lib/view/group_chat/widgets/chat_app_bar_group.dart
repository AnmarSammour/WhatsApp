import 'package:flutter/material.dart';

class ChatAppBarGroup extends StatelessWidget {
  const ChatAppBarGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}