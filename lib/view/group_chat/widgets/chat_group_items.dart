import 'package:flutter/material.dart';

class ChatGroupItems extends StatelessWidget {
  const ChatGroupItems({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}