import 'package:flutter/material.dart';

class StoryProgressPainter extends StatelessWidget {
  const StoryProgressPainter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}